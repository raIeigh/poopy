p:eval marios = []
mariotokens.forEach(tok => {
var mario = new poopy.modules.Discord.Client({
    intents: [
        poopy.modules.Discord.Intents.FLAGS.DIRECT_MESSAGES,
        poopy.modules.Discord.Intents.FLAGS.GUILDS,
        poopy.modules.Discord.Intents.FLAGS.DIRECT_MESSAGE_REACTIONS,
        poopy.modules.Discord.Intents.FLAGS.DIRECT_MESSAGE_TYPING,
        poopy.modules.Discord.Intents.FLAGS.GUILD_BANS,
        poopy.modules.Discord.Intents.FLAGS.GUILD_EMOJIS_AND_STICKERS,
        poopy.modules.Discord.Intents.FLAGS.GUILD_INTEGRATIONS,
        poopy.modules.Discord.Intents.FLAGS.GUILD_INVITES,
        poopy.modules.Discord.Intents.FLAGS.GUILD_MESSAGES,
        poopy.modules.Discord.Intents.FLAGS.GUILD_MESSAGE_REACTIONS,
        poopy.modules.Discord.Intents.FLAGS.GUILD_MESSAGE_TYPING,
        poopy.modules.Discord.Intents.FLAGS.GUILD_VOICE_STATES,
        poopy.modules.Discord.Intents.FLAGS.GUILD_WEBHOOKS
    ]
})
marios.
var shut = []
mario.on("message",(masg)=>{
if (masg.channel.type ==='dm') return
if (masg.content.includes('shut')) {shut.push(masg.author.id)}
if(masg.mentions.members.size){
if (masg.mentions.members.find(ment => ment.id === mario.user.id) && !(shut.find(shit => shit === masg.author.id))){
var int = setInterval(() => {
if (shut.find(shit => shit === masg.author.id)) {clearInterval(int)}
masg.author.send('i love tax evasion.').catch(()=>{})
}, 1500)

} }
})
mario.login(tok)
})

'run'